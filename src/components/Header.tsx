import React, { FC } from 'react';

const Header: FC = () => {
    return (
        <header className="App-header">
            <h1>Тайны Аркхэма</h1>
        </header>
    );
};

export default Header;